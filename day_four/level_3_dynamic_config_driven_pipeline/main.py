from cli import app

# Entry point for the CLI application
if __name__ == "__main__":
    """
    This block ensures that the CLI app runs only when the script
    is executed directly, not when it's imported as a module.
    """
    app()
